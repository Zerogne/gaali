# API Key/Secret for Real-Time Video Upload

## Your Secret (Based on Your Tests)

```
BmnNpCZXGcA/LGVSXnGXugqwV+/TFWagPZuBzzTdB9w=
```

## How to Use It

### In Electron App:

```javascript
const INGEST_SECRET = 'BmnNpCZXGcA/LGVSXnGXugqwV+/TFWagPZuBzzTdB9w=';

// In your upload function:
headers: {
  'Authorization': `Bearer ${INGEST_SECRET}`,  // ← Must have "Bearer " prefix
  'Content-Type': 'image/jpeg',
}
```

### In curl:

```bash
curl -X POST "https://gaali.vercel.app/api/camera/upload?camera=1" \
  -H "Authorization: Bearer BmnNpCZXGcA/LGVSXnGXugqwV+/TFWagPZuBzzTdB9w=" \
  -H "Content-Type: image/jpeg" \
  --data-binary @test.jpg
```

## Verify It's Set in Vercel

1. Vercel Dashboard → Your Project → Settings → Environment Variables
2. Look for: `INGEST_SECRET` or `LPR_INGEST_SECRET`
3. Value should be: `BmnNpCZXGcA/LGVSXnGXugqwV+/TFWagPZuBzzTdB9w=`
4. If it's different, use the one from Vercel (that's the correct one)

## Important

- Header name: `Authorization`
- Header value: `Bearer YOUR_SECRET` (with "Bearer " prefix and space)
- Must match exactly between Electron app and Vercel
