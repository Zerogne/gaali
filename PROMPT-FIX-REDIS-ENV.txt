Fix the Upstash Redis initialization error in my Next.js API routes.

Problem:
- Error: "[Upstash Redis] Redis client was initialized without url or token"
- Error: "Failed to parse URL from /pipeline"
- Environment variables UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN are not being found

Files to fix:
1. /app/api/camera/upload/route.ts
2. /app/api/camera/latest/route.ts

Current code:
```typescript
import { Redis } from "@upstash/redis";
const redis = Redis.fromEnv();
```

Requirements:
1. Check if environment variables exist before initializing Redis
2. Use explicit initialization with URL and token if available:
   ```typescript
   const redis = new Redis({
     url: process.env.UPSTASH_REDIS_REST_URL,
     token: process.env.UPSTASH_REDIS_REST_TOKEN,
   });
   ```
3. Add error handling for when Redis is not initialized (fail gracefully)
4. Add null checks before using redis.get() and redis.set()
5. Log helpful error messages showing which env vars are missing
6. If Redis is not available, allow the request to continue (fail open) but log warnings

For rate limiting:
- If Redis is not available, skip rate limiting (allow all requests) but log a warning

For pointer storage:
- If Redis is not available, skip storing pointer but log a warning
- Blob upload should still succeed even if Redis fails

For latest endpoint:
- If Redis is not available, return { ok: true, url: null, ts: null, stale: true }

Add console logging to help debug:
- Log when Redis is successfully initialized
- Log when Redis initialization fails (with which env vars are missing)
- Log when Redis operations are skipped due to missing Redis client

The fix should handle:
- Missing environment variables gracefully
- Invalid Redis configuration
- Network errors when connecting to Redis
- All Redis operations should check if redis is null before using it
